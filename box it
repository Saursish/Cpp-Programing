#include<bits/stdc++.h>
using namespace std;
class Box{
  int len,hgt,bdth;
  public:
  Box(){len=0;hgt=0;bdth=0;};
  //Box(Box &b){b.len=len;b.hgt=hgt;b.bdth=bdth;  }
  Box(int a,int c,int d)
  {
      len=a;
      bdth=c;hgt=d;
  } 
  int getLenth(){return len;}
  int getBreadth(){return bdth;}
  int getHeight(){return hgt;}
  long long CalculateVolume(){return (long long)len*hgt*bdth;}
  friend ostream& operator<<(ostream& out, Box& B);
  bool operator<(Box& b);
};
bool Box :: operator<(Box& b)
{
    if(len<b.len){return true;}
    else if(len==b.len&&bdth<b.bdth){return true;}
    else if(len==b.len&&bdth==b.bdth&&hgt<b.hgt){return true;}
    else{return false;}
}
 ostream &operator<<(ostream& out, Box &B)
{
    cout<<B.len<<" "<<B.bdth<<" "<<B.hgt;
    return out;
}


void check2()
{
	int n;
	cin>>n;
	Box temp;
	for(int i=0;i<n;i++)
	{
		int type;
		cin>>type;
		if(type ==1)
		{
			cout<<temp<<endl;
		}
		if(type == 2)
		{
			int l,b,h;
			cin>>l>>b>>h;
			Box NewBox(l,b,h);
			temp=NewBox;
			cout<<temp<<endl;
		}
		if(type==3)
		{
			int l,b,h;
			cin>>l>>b>>h;
			Box NewBox(l,b,h);
			if(NewBox<temp)
			{
				cout<<"Lesser\n";
			}
			else
			{
				cout<<"Greater\n";
			}
		}
		if(type==4)
		{
			cout<<temp.CalculateVolume()<<endl;
		}
		if(type==5)
		{
			Box NewBox(temp);
			cout<<NewBox<<endl;
		}

	}
}

int main()
{
	check2();
}
